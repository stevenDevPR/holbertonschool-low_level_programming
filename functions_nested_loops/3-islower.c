#include "main.h"
/**
 * clower - function main
 *
 * Return: 0
 */

int _islower(int c);

int clower(void)
{
	_islower();

	return (0);

}

/**
 * _islower - finding if c is lower
 */

int _islower(int c)
{

	if (c == 'c')
	{
		return (1);
	}
	else
		return (0);
}
