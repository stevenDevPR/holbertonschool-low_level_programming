#include "main"
/**
 * islower - function main
 *
 * Return: 0
 */

int _islower(int c);

int islower(void)
{
	is_lower();

	return (0);

}

/**
 * _islower - finding if c is lower
 */

int _islower(int c)
{
	int c;

	if (c == 99)
	{
		return (1);
	}
	else
		return (0);
}
