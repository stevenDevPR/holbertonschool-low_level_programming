#include <stdio.h>

/**
 * int main(void)= main function
 * const char *message= message as a constant with character type
 * return (0) = when finish correctly without error will return 0, if error is present it will return 1 */

int main(void)
{
	const char *message = "\"Programming is like building a multilingual puzzle";

	puts(message);

	return (0);

}
